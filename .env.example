# Optional: public site URL (no trailing slash) for Open Graph metadata in layout.tsx.
# Local: http://localhost:3000
# Production: https://aurum-website-beta.vercel.app (or your custom domain)
NEXT_PUBLIC_SITE_URL=http://localhost:3000
